ingress-nginx:
    默认安装用的是 deployment，建议改成DeamonSet
    原因是因为deployment有副本数，DeamonSet是每个node上安装，那么要是想扩展的话，直接在那个node上加入标签就可以（前提是安装的时候指定了ingree-nginx的nodeSeletor）
    如果不想在一个node上使用ingress-nginx，则删掉这个标签就可以，很方便
    而如果用 Deployment的话，就不会自动的扩展，因为副本数决定了不会多的扩展
